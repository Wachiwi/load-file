name: load-file
version: 0.1.0

authors:
  - David Schwarzmann <dev@dschwarz.codes>

targets:
  load-file:
    main: src/load-file.cr

dependencies:
  cli:
    github: mosop/cli
  gitlab:
    github: icyleaf/gitlab.cr
    branch: master

crystal: 0.23.0

license: MIT
