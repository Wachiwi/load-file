name: load-file
version: 0.1.0

authors:
  - David Schwarzmann <dev@dschwarz.codes>

targets:
  load-file:
    main: src/load-file.cr

dependencies:
  commander:
    github: mrrooijen/commander
    version: ~> 0.3.3
  gitlab:
    github: icyleaf/gitlab.cr
    branch: master
      
crystal: 0.22.0

license: MIT
